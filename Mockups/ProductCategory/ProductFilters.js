
class ProductFilters extends Component {
  constructor (props) {
    super(props)

    this.filters = { ...this.props.filters } // create new writable filters object
  }

  renderFilters () {
    this.filters.map(filter => {

    })
  }
}
